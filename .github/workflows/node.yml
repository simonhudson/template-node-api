# This workflow will do a clean installation of node dependencies, cache/restore them, build the source code and run tests across different versions of node
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-nodejs

name: Node.js CI

on:
    pull_request:
        branches: 'main'
        types:
            - closed
env:
    GH_TOKEN: ${{ github.token }}

jobs:
    build:
        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [18.x] # See supported Node.js release schedule at https://nodejs.org/en/about/releases/

        steps:
            - uses: actions/checkout@v3
            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v3
              with:
                  node-version: ${{ matrix.node-version }}
                  cache: 'npm'

            # Set outputs
            - name: Set outputs
              id: vars
              run: echo "commit_sha_short=$(git rev-parse --short HEAD) release_branch_name=release" >> $GITHUB_OUTPUT
            - name: Check outputs
              run: echo ${{ steps.vars.outputs.release_branch_name }}

            # Install dependencies
            - run: npm ci

            # Provide GH creds
            - run: git config user.email "hellosimonhudson@gmail.com"
            - run: git config user.name "Simon Hudson"

            # Create temporary branch
            - run: git checkout -b ${{ steps.vars.outputs.release_branch_name }}

            # Build
            - run: npm run build --if-present

            # Add and Push /dist to main branch
            - run: git add dist
            - run: git commit -m "build dist"
            - run: git push

            # Push temporary branch
            - run: git push origin ${{ steps.vars.outputs.release_branch_name }}

            ## Create release
            - run: gh release create ${{ steps.vars.outputs.commit_sha_short }} --title ${{ steps.vars.outputs.commit_sha_short }} --notes "Pull Request \#${{ github.event.number }}"

            # Delete temporary branch
            - run: git checkout main
            - run: git branch -d ${{ steps.vars.outputs.release_branch_name }}
